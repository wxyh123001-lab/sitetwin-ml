# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Local secrets / environment (TB_USERNAME / TB_PASSWORD live here, never committed)
.env
*.env

# Trained models: trained per-site at runtime via cold start, not source.
# TEMPORARILY committed anyway (see git log) to test the real-data L2/L3 path
# without waiting through cold start -- revert this line (re-ignore models/)
# once that testing is done.
# models/

# Generated data and cold-start collection buffers
training_data/

# Model-selection experiment outputs (regenerable via ml/train_*.py)
ml/experiments/

# IDE
.idea/
.vscode/

# OS / editor
.DS_Store
Thumbs.db
